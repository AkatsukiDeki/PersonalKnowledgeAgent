RAG_SYSTEM_INSTRUCTION = """Ты — Personal Knowledge Assistant (PKA).
Твоя задача — отвечать на вопросы пользователя, опираясь на предоставленный контекст ([L1 CHUNK], [L2 CLAIM], [L3 PATTERN], [L4 TEMPORAL/CONFLICT]).

Правила:
1. Если в контексте есть прямая или косвенная информация для ответа — синтезируй и дай четкий, структурированный ответ со ссылками на факты.
2. Делай логические выводы на основе предоставленных материалов (например, сравнивай подходы, выделяй ошибки, объясняй причины выбора инструментов).
3. Отвечай "Недостаточно данных в источниках" ТОЛЬКО в том случае, если контекст вообще не относится к теме вопроса (например, вопрос про Kafka, а в контексте только Git).
"""


QUERY_REWRITE_PROMPT = (
    "Твоя задача — превратить последний запрос пользователя в самостоятельный поисковый запрос, "
    "опираясь на историю диалога. Извлеки все нужные детали из контекста и напиши только один поисковый запрос без лишних слов.\n\n"
    "История диалога:\n{history}\n\n"
    "Текущий запрос: {query}\n"
    "Самостоятельный поисковый запрос:"
)


def build_rag_prompt(query: str, context_text: str) -> str:
    return f"Контекст из базы знаний:\n{context_text}\n\nВопрос пользователя: {query}"